const { Telegraf } = require('telegraf');\nconst OpenAI = require('openai');\nconst { exec } = require('child_process');\nconst util = require('util');\nconst execPromise = util.promisify(exec);\n\nrequire('dotenv').config();\n\nconst bot = new Telegraf(process.env.TELEGRAM_TOKEN);\nconst nim = new OpenAI({\n  apiKey: process.env.NVIDIA_NIM_API_KEY,\n  baseURL: 'https://api.nvidia.com/v1',\n});\n\nbot.start((ctx) => ctx.reply('¡Hola! Usa /nemotron &lt;mensaje&gt; para Nemotron o /openclaw &lt;mensaje&gt; para OpenClaw.'));\n\nbot.command('nemotron', async (ctx) => {\n  const msg = ctx.message.text.split(' ').slice(1).join(' ');\n  if (!msg) return ctx.reply('Proporciona un mensaje después de /nemotron');\n  try {\n    const completion = await nim.chat.completions.create({\n      model: 'meta/llama-3.1-nemotron-70b-instruct',\n      messages: [{ role: 'user', content: msg }],\n    });\n    ctx.reply(completion.choices[0]?.message?.content || 'No response');\n  } catch (error) {\n    ctx.reply(`Error con Nemotron: ${error.message}`);\n  }\n});\n\nbot.command('openclaw', async (ctx) => {\n  const msg = ctx.message.text.split(' ').slice(1).join(' ');\n  if (!msg) return ctx.reply('Proporciona un mensaje después de /openclaw');\n  try {\n    // Asume OpenClaw gateway o CLI configurado con NVIDIA\n    const { stdout } = await execPromise(`openclaw capability chat --provider nvidia --model ${process.env.OPENCLAW_MODEL || 'meta/llama-3.1-nemotron-70b-instruct'} --prompt \\\"${msg}\\\"`);\n    ctx.reply(stdout.trim());\n  } catch (error) {\n    ctx.reply(`Error con OpenClaw: ${error.message}`);\n  }\n});\n\nbot.launch();\nconsole.log('Bot lanzado');\n\nprocess.once('SIGINT', () => bot.stop('SIGINT'));\nprocess.once('SIGTERM', () => bot.stop('SIGTERM'));\n